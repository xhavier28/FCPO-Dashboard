======================================================================
FCPO CALENDAR SPREAD — BACKTEST DASHBOARD BUILD LOG
======================================================================

Generated: 2026-07-20
Updated:   2026-07-20 (v2 — all 6 spec gaps fixed)


--- A.5 VALIDATION: OLD COST (100 MYR flat) ---

Config: z>1.5, exit_z=0.5, dur>=3d, lots=1, time_stop=20d,
        no stop-loss, no scale-in, PM L0, TM RR<50%,
        9 instruments, cost=100 MYR flat (4.0 pts/lot).

  W1 (2019-2020): n=156, PnL=+1,395.0, adj Sharpe=1.633 — PASS
  W2 (2021-2022): n=133, PnL=+6,131.0, adj Sharpe=2.540 — PASS
  W3 (2023-2024): n=126, PnL=+1,437.0, adj Sharpe=1.167 — PASS
  W4 (2025-2026): n= 72, PnL=  +700.0, adj Sharpe=1.215 — PASS

  All 4 windows match published RR<50% numbers EXACTLY.
  Validated AGAIN after v2 engine changes — still exact match.


--- A.6 VALIDATION: NEW COST (spread=22, butterfly=44 MYR) ---

Config: Same as A.5 but with differentiated costs:
  - Spread:    22 MYR/lot RT → 0.88 pts/lot
  - Butterfly: 44 MYR/lot RT → 1.76 pts/lot

  W1 (2019-2020): n=156, PnL=+1,811.3, adj Sharpe=2.113
  W2 (2021-2022): n=133, PnL=+6,478.2, adj Sharpe=2.665
  W3 (2023-2024): n=126, PnL=+1,772.0, adj Sharpe=1.408
  W4 (2025-2026): n= 72, PnL=  +893.8, adj Sharpe=1.529

  Trade counts unchanged (cost only affects PnL, not entry/exit).
  Worst-case adj Sharpe improved from 1.167 to 1.408.


--- BUTTERFLY POINT VALUE CONFIRMATION ---

POINT_VALUE = 25.0 MYR/point for BOTH spreads and butterflies.
Source: stage2_walkforward.py line 33, stage2_tm_threshold_retest.py
line 28, stage2_progressive_scalein.py — all use POINT_VALUE=25.0
with no separate butterfly point value defined anywhere.

Butterfly cost is 2x spread cost (44 vs 22 MYR) because butterflies
have 4 legs vs 2 legs, but the point value per tick is the same.


--- NAIVE + ADJUSTED SHARPE ---

Both Sharpe ratios now computed and displayed:
  Naive Sharpe: attributes each trade's net PnL to its exit date
  Adjusted Sharpe: daily mark-to-market portfolio PnL reconstruction

Published numbers at old cost:
  W1: naive=3.160, adj=1.633
  W2: naive=3.599, adj=2.540
  W3: naive=1.610, adj=1.167
  W4: naive=1.761, adj=1.215


--- LOSS BUCKET B/C SEPARATION ---

Now properly separated using max|z| tracked during each trade:
  A = invalidated (regime break)
  B = time_stop AND max|z| > |entry_z| (adverse extension)
  C = time_stop AND max|z| <= |entry_z| (stalled)
  D = regime_risk
  TP-loss = take_profit with net_pnl <= 0 (cost-induced)
  SL = stop_loss

Verified at old cost:
  W1: A=20, B=7, C=5, TP-loss=4
  W2: A=22, TP-loss=3
  W3: A=18, B=6, C=1, TP-loss=6
  W4: A=21, TP-loss=3


--- 60-MIN INTRADAY SUPPORT ---

Data source: Pre-computed wide CSVs from Stage 3 rebuild:
  spread_tenor_close_wide.csv    (5033 bars, 2024-01-02 to 2026-07-16)
  butterfly_tenor_close_wide.csv (5412 bars, 2024-01-02 to 2026-07-16)

Tenor → instrument mapping (direct, no differencing):
  Spread: Current=M1-M2, +1M=M2-M3, +2M=M3-M4, +3M=M4-M5, +4M=M5-M6
  Butterfly: Current=BF_M1M2M3, +1M=BF_M2M3M4, +2M=BF_M3M4M5, +3M=BF_M4M5M6

Z-score approach: daily mean/std baseline, intraday bar price for
threshold crossings (entry, TP, SL, scale-in). Daily-level gates
(shape, PM, TM, duration) checked once per day before scanning bars.


--- DESIGN DECISIONS ---

1. TM threshold = 0.50
   At threshold=0.50, TM regime-risk exit rarely fires (0-3 exits
   per window). Selected as best worst-case config.

2. Cost model
   Cost is deducted ONCE per full position close, per lot.
   cost_points = cost_myr / POINT_VALUE
   For scale-in: total_cost = total_lots * cost_per_lot

3. MtM Sharpe (adj Sharpe)
   For OLD cost validation: uses 100 MYR flat for all instruments
   (matching original code exactly for reproducibility).
   For NEW cost: uses differentiated costs per instrument type.

4. Panel caching
   Data panel (4070 rows, 2008-2026) built once, cached in-memory.
   PM/TM predictions (~2016 each) also cached.

5. Dashboard pre-caching
   Locked-config daily results saved to locked_config_cache.pkl.
   Intraday results computed on demand (fast: no PM/TM recomputation
   needed, uses cached panel).

6. Scale-in UI
   3 independent tier rows, each with its own enable checkbox,
   z-level input, and lot-size input. Enabling tier 1 and 3 but
   not 2 is supported (per-tier independence).

7. Two-tab layout
   Tab 1: "Daily (4 Walk-Forward Windows)" — all 4 windows side
          by side, worst-case Sharpe highlighted.
   Tab 2: "60-Min Intraday (2024-2026)" — single continuous range.
   Parameters are SHARED across both tabs.

8. Intraday exit priority
   Daily-level exits (shape change, time stop, TM regime-risk) are
   checked FIRST at the start of each day using daily settlement.
   If no daily exit, intraday bars are scanned for TP, SL, and
   scale-in triggers within that day.


--- URGENT: CONTRACT PINNING VERIFICATION ---
Date: 2026-07-20

FINDING: Behavior (b) confirmed — ROLLING LABELS, NOT PINNED CONTRACTS.

The engine reads df[inst] (e.g., df['M1-M2']) on every day of an open
trade. Because M1, M2, ... M6 in the daily shape log are rolling tenor
labels (relative to front_month(date)), the actual underlying contract
pair CHANGES at each roll date (day 16 of the month). This means an
open position silently switches to a different contract pair mid-trade.

Root cause:
  backtest_engine.py line 341:
    date, shape, z, spread = row['date'], row['shape'], row[z_col], row[inst]
  This reads the rolling label value on EACH day, not the contract
  that was live at entry.

This is NOT a new engine bug — the ORIGINAL source code
(stage2_walkforward.py lines 239-316) uses the exact same data
construction (df[name] = df[cfg['near']] - df[cfg['far']] where
M1-M6 are rolling labels). The engine faithfully reproduces the
original behavior, which is why validation passes exactly.

QUANTIFICATION:

  Total trades (all 4 windows, old cost): 487
  Trades crossing a roll date:            176 (36.1%)
  Trades NOT crossing a roll date:        311 (63.9%)

  By window:
    W1 (2019-2020): 156 total, 55 affected (35.3%)
    W2 (2021-2022): 133 total, 44 affected (33.1%)
    W3 (2023-2024): 126 total, 55 affected (43.7%)
    W4 (2025-2026):  72 total, 22 affected (30.6%)

  PnL breakdown:
    Total PnL (old cost):   +9,663.0 pts
    Affected trades PnL:    +4,653.0 pts (48.2% of |total|)
    Unaffected trades PnL:  +5,010.0 pts

  Win rate comparison:
    Affected:   76.1% (134/176), avg PnL = +26.44 pts/trade
    Unaffected: 76.2% (237/311), avg PnL = +16.11 pts/trade

  Roll-date jump statistics (176 roll crossings):
    Mean |jump|:   38.4 pts
    Median |jump|: 17.0 pts
    Max |jump|:    322.0 pts
    Jumps > 20 pts: 77 (43.8%)
    Jumps > 50 pts: 40 (22.7%)

  By instrument type:
    Spreads:     mean |jump| = 36.6 pts, max = 322.0 pts
    Butterflies: mean |jump| = 40.6 pts, max = 274.0 pts

INTERPRETATION:

  The jumps are large (mean 38.4 pts, max 322 pts) and represent
  discontinuities where the engine's "price" for an open position
  suddenly shifts to a different contract pair. These jumps are NOT
  real market movements — they are artifacts of the rolling label
  switching to the next contract month.

  Notably, affected trades show HIGHER avg PnL (+26.44 vs +16.11)
  which suggests some roll-date jumps coincidentally align with the
  trade direction, inflating reported returns. However, this could
  also go the other way — the distortion is unpredictable and
  depends on the contango/backwardation structure at each roll.

  Since the bug exists in the original stage2_walkforward.py code,
  ALL published Stage 2 numbers (including the validated W1-W4
  figures above) contain this distortion. The validation PASSES
  because both codebases have the same bug.

STATUS: Report only. No fix applied.
  The scope of the fix (and whether it needs to be re-applied
  retroactively to Stage 1/2's published numbers) is a decision
  to make together once the actual impact is understood.


--- FILE MANIFEST ---

Created:
  MRBackTest/engine/backtest_engine.py          — Parameterized engine
  MRBackTest/FCPO Backtest Dashboard/app.py     — Streamlit dashboard
  MRBackTest/FCPO Backtest Dashboard/.streamlit/config.toml — Dark theme
  MRBackTest/FCPO Backtest Dashboard/locked_config_cache.pkl — Pre-cached
  MRBackTest/FCPO Backtest Dashboard/dashboard_build_log.txt — This file

Read-only references:
  research/05. stage2_walkforward/stage2_walkforward.py
  research/05. stage2_walkforward/stage2_tm_threshold_retest.py
  research/05. stage2_walkforward/stage2_progressive_scalein.py
  research/06. stage3_minute_rebuild/spread_tenor_close_wide.csv
  research/06. stage3_minute_rebuild/butterfly_tenor_close_wide.csv
  models/pm_engine.py
  models/tm_engine.py
  models/feature_prep.py


--- Contract Pinning Fix (2026-07-20) ---
Fixed contract-pinning bug: open positions now use fixed contract months
resolved at entry, not rolling tenor labels. See stage2_walkforward_log.txt
"CORRECTED RE-VALIDATION" section for full before/after comparison.
Worst-case adj Sharpe: -0.153 (corrected)


======================================================================
M1-M2 EXPIRY BUFFER FIX — 2026-07-21
======================================================================

Problem: M1-M2 near-leg contract stops trading ~day 12-15 of delivery
month. Shared spread roll rule (day 16) kept referencing the expired
contract through day 15, causing NaN → fallback to rolling-label pricing.
488 of 512 total fallback events across the full dataset were M1-M2.

Fix applied to BOTH daily and minute (intraday 60-min) engine paths in
MRBackTest/engine/backtest_engine.py:
  - New function: m1m2_expiry_exit_due()
  - Rule: force-close any open M1-M2 position on or after day 8 of the
    near-leg delivery month (= 7 calendar days before nominal 15th LTD
    per Bursa Malaysia FCPO contract specs)
  - Exit reason: 'expiry_buffer'
  - Also blocks new M1-M2 entries during the buffer zone
  - Isolated to M1-M2 only; no other instruments affected
  - Sits alongside existing exits (TP, invalidated, time stop, TM,
    stop loss); whichever triggers first fires; expiry_buffer is checked
    LAST so other exits take priority if they fire same day

Constants added:
  M1M2_EXPIRY_BUFFER_DAYS = 7
  M1M2_FORCE_CLOSE_DAY = 8  (15 - 7)

Validation (daily engine, all 4 windows):
  Fallback events before fix: 123 (9 entry + 114 hold)
  Fallback events after fix:  6 (3 entry + 3 hold) — all in 2019,
    genuine CSV gaps not related to roll-date mismatch
  Reduction: 95.1%

Validation (minute/intraday engine, 2024-2026):
  Fallback events after fix: 0
  M1-M2 trades with fix: 12 (5 expiry_buffer, 4 take_profit, 3 invalidated)

Shared code: fix lives in backtest_engine.py, used by both _run_window_daily
and _run_window_intraday. The minute pipeline (stage3_minute_rebuild.ipynb)
imports roll logic from MRBackTest/shared/tenor_mapping.py — same shared
code, fix applied at engine level not at data-build level.


======================================================================
TENOR-LADDER INTEGRATION (GENERIC EXPIRY EXIT) — 2026-07-21
======================================================================
SUPERSEDES the M1-M2-only m1m2_expiry_exit_due() above (preserved for
history). Old function kept as backward-compatibility alias.

Change: replaced M1-M2-specific expiry exit with generic expiry_exit_due()
that checks ALL legs of ANY instrument against the expiry buffer zone.
  - Function: expiry_exit_due(inst, resolved_contracts, date)
  - Rule: for each leg (year, month) in resolved_contracts, if
    date.year == year and date.month == month and date.day >= 8, exit.
  - Constants: EXPIRY_BUFFER_DAYS=7, EXPIRY_FORCE_CLOSE_DAY=8
  - Applied to both _run_window_daily and _run_window_intraday
  - Also blocks new entries when any resolved leg is in buffer zone

Design decisions:
  - Butterfly's day-2 roll rule (ROLL_DAY_BUTTERFLY=1) KEPT as-is —
    it's a tenor-label mapping rule, independent of position exit.
    The generic expiry exit is a position exit rule, applied on top.
  - In practice, only M1-M2 triggers expiry_exit_due because all other
    instruments have near legs >= 1 month from LTD at entry.
  - The generic check provides a safety net for all 9 instruments.

Downstream consumers NOT changed: z-score computation, daily panel
columns, PM/TM features, intraday bar loading, dashboard tabs. Only
the expiry exit check was generalized.

Validation (minute data, 2024-2026, all 9 instruments):
  Fallback events before: 3 (all M1-M2)
  Fallback events after:  0
  Trade count: 113 (identical to M1-M2-only fix)
  Adj Sharpe:  0.693 (identical, delta +0.000)
  Expiry buffer exits: 5 (all M1-M2, as expected)


======================================================================
V2 MINUTE-DATA DEBUG DASHBOARD — 2026-07-22
======================================================================

PURPOSE: Single-tab, minute-data-only dashboard for inspecting exit
reasons and interactively tweaking entry/exit z-score thresholds.
Separate from v1 (app.py on port 8506) — v1 NOT modified.

FILE: MRBackTest/FCPO Backtest Dashboard/app_v2.py
PORT: 8507
RUN:  python -m streamlit run "MRBackTest/FCPO Backtest Dashboard/app_v2.py" --server.port 8507

ARCHITECTURE:
  - Single tab, no daily data tab
  - Reuses validated backtest engine (backtest_engine.py) — no
    duplicated logic
  - Reuses tenor_mapping.py for calendar-month labeling
  - Imports engine via importlib (same pattern as v1)

FEATURES:
  1. Calendar-month tenor tracking chart (collapsible expander)
     - Each line = one calendar delivery month contract (e.g. Jan26)
     - Lines cut off at expiry-buffer deadline (day 8 of delivery month)
     - Color-coded by delivery month
  2. Per-trade exit-reason table with filtering
     - Expiry_buffer trades highlighted in cyan
     - Filterable by instrument, exit reason, date range
  3. Aggregate exit reason breakdown (table + pie chart)
  4. Per-instrument summary with exit reason counts
  5. Interactive entry/exit/duration threshold controls
     - Defaults to locked baseline (entry=1.5, exit=0.5, duration>=3d)
     - "Best Known" button jumps to entry=1.75/exit=0.25
     - Live re-run updates portfolio + per-instrument metrics
  6. Cost model defaults to new cost (spread=22, butterfly=44 MYR)

VALIDATION (2026-07-22):

  Reference numbers were generated at OLD COST (100 MYR flat).
  v2 dashboard defaults to NEW COST (spread=22, butterfly=44 MYR).
  Engine behavior is IDENTICAL — only cost affects PnL/Sharpe.

  At OLD COST (100 MYR flat):
    Baseline (1.5/0.5): n=113, win=52.2%, PnL=+408.0, adj Sharpe=0.693, max DD=-160.0 — EXACT MATCH
    Best (1.75/0.25):   n=80,  PnL=+407.0, adj Sharpe=0.974, max DD=-96.0 — EXACT MATCH

  At NEW COST (spread=22, butterfly=44 MYR):
    Baseline (1.5/0.5): n=113, win=67.3%, PnL=+728.0, adj Sharpe=1.366, max DD=-81.6
    Best (1.75/0.25):   n=80,  PnL=+632.8, adj Sharpe=1.484, max DD=-60.9

  Trade counts identical across both cost models (cost does not affect
  entry/exit decisions). PnL/Sharpe differ only due to cost deduction.

v1 STATUS: NOT MODIFIED. v2 is a new, separate, single-purpose file.
